name: todo-write-service
image: todo-write-service
tag: latest
port: 3000
environment:
  DB_HOST: postgres
  DB_USER: todouser
  DB_PASSWORD: todopass
  DB_NAME: todoapp
  DB_PORT: 5432
  NODE_ENV: production
  PORT: 3000
dependencyGroups:
  - database
routes:
  - methods: [POST, PUT, DELETE, PATCH]
    paths: ["/api/todos/*"]