{
  "name": "todo-services-e2e-tests",
  "version": "1.0.0",
  "description": "End-to-end tests for todo read and write services",
  "main": "index.js",
  "scripts": {
    "test": "jest --runInBand",
    "test:ci": "jest --runInBand --ci --detectOpenHandles --reporters=default --reporters=jest-junit",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "test:verbose": "jest --verbose"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "pg": "^8.11.0",
    "xml2js": "^0.6.2"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "jest": "^29.5.0",
    "jest-junit": "16.0.0"
  }
}